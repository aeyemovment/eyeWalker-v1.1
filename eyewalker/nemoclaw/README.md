# NemoClaw (optional) + Omniverse (optional)

**eyeWalker v1.1.9 simulated accessibility-interface research** — NeuroAgent AI.

## Honesty

| Component | Status |
|-----------|--------|
| NemoClaw | **Optional, unenforced policy notes** plus illustrative YAML. There is no runtime integration in this repository. Do **not** market it as a security product or data-egress guarantee. |
| Omniverse | **Optional stub** in `eyewalker/omniverse/` — disabled by default; returns `available: false` until you install Omniverse/Isaac yourself. |

Full NVIDIA NemoClaw lifecycle (CLI, OpenShell, versioned blueprints) is **out of scope** until implemented and tested against current official NVIDIA documentation. The included YAML is not loaded or validated by eyeWalker.

## PWA

The browser PWA runs **without** NemoClaw or Omniverse.

## Privacy intent

Privacy intent: prefer local processing and require explicit user consent before any future remote path. This intent is not technical enforcement or a cryptographic guarantee.
